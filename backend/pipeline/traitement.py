from flask import Flask


def traiter_hydrometeorologie(app: Flask, lst_donnees: list[dict]):
    with app.app_context():
        ""


def traiter_hydrometrie(app: Flask, lst_donnees: list[dict]):
    with app.app_context():
        ""
